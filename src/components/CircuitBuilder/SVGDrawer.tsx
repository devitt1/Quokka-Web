/**
 * Draw the nodes.
 * Each time this is called we only draw the added nodes since we are using "enter" only
 */
class SVGDrawer {
    static draw(nodes : any) {
        console.log("somethings");
    }
}

export default SVGDrawer;
